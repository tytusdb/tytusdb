import instrucciones as inst
import reglas as r

class Salto():
    def __init__(self, etiqueta):
        self.etiqueta = etiqueta

    def optimizacion(self):
        pendiente.append(self.etiqueta)
        return ''